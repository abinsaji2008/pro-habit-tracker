import React,{useEffect,useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{CalendarDays,Clock3,MapPin,Plus,Settings2,ChevronLeft,ChevronRight,Check,X,RefreshCw,Cloud,CloudOff,ShieldCheck,Trash2,Pencil,Home,BookOpen,Cpu,Dumbbell,School,Coffee,Monitor,CircleDot,ArrowRight,ExternalLink,Save,LayoutDashboard,CheckSquare,StickyNote,BarChart3}from'lucide-react';
import{initializeApp}from'firebase/app';
import{getAuth,GoogleAuthProvider,onAuthStateChanged,signInWithPopup,signOut}from'firebase/auth';
import{getFirestore,collection,doc,setDoc,deleteDoc,onSnapshot,query,orderBy}from'firebase/firestore';
import{getDatabase,ref,get,set,remove}from'firebase/database';
import'./styles.css';
const firebaseConfig={apiKey:'AIzaSyAipiHiXzu4mSvK6BbNjImOHYdkqxKSt3c',authDomain:'tracking-98a71.firebaseapp.com',databaseURL:'https://tracking-98a71-default-rtdb.firebaseio.com',projectId:'tracking-98a71',storageBucket:'tracking-98a71.firebasestorage.app',messagingSenderId:'841004547092',appId:'1:841004547092:web:129ea46b2297ce01c0fb01',measurementId:'G-XQFRPXB3J9'};
const GOOGLE_CLIENT_ID='728358241274-6t5jde621j1i0gdeckviiokihvl1ikck.apps.googleusercontent.com';
const DAYS=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'],DAYKEY=['MO','TU','WE','TH','FR','SA','SU'];
const COMMON=[['Study Desk',Monitor],['Bedroom',Home],['School',School],['Library',BookOpen],['Workshop',Cpu],['Gym',Dumbbell],['Kitchen',Coffee]];
const TYPES={routine:['Routine',CircleDot],study:['Study',BookOpen],fixed:['Fixed',School],personal:['Personal',Home]};
const EMPTY={tasks:{},habits:{},dailyRecords:{}};
const pad=n=>String(n).padStart(2,'0'),mins=t=>{const[a,b]=t.split(':').map(Number);return a*60+b},clock=m=>{const h=Math.floor(m/60);return`${h%12||12}:${pad(m%60)} ${h>=12?'PM':'AM'}`},duration=m=>{const h=Math.floor(m/60),r=m%60;return h?`${h}h${r?' '+r+'m':''}`:`${r}m`},dayIndex=d=>d.getDay()===0?6:d.getDay()-1,dateKey=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
function useFirebase(){const[ctx,setCtx]=useState(null),[user,setUser]=useState(null);useEffect(()=>{const app=initializeApp(firebaseConfig),auth=getAuth(app),db=getFirestore(app),rtdb=getDatabase(app);return onAuthStateChanged(auth,u=>{setUser(u);setCtx({auth,db,rtdb})})},[]);return{ctx,user}}
function App(){const{ctx,user}=useFirebase(),[tab,setTab]=useState('today'),[selected,setSelected]=useState(new Date()),[blocks,setBlocks]=useState([]),[legacy,setLegacy]=useState(EMPTY),[places,setPlaces]=useState(['Study Desk','Bedroom','School','Workshop']),[modal,setModal]=useState(null),[notice,setNotice]=useState(''),[token,setToken]=useState(null),[connected,setConnected]=useState(false),[events,setEvents]=useState([]),[loading,setLoading]=useState(false);
useEffect(()=>{if(!ctx||!user)return;const q=query(collection(ctx.db,'users',user.uid,'blocks'),orderBy('start'));return onSnapshot(q,s=>setBlocks(s.docs.map(d=>({id:d.id,...d.data()}))))},[ctx,user]);
useEffect(()=>{if(!ctx||!user)return;get(ref(ctx.rtdb,`users/${user.uid}`)).then(s=>{const v=s.exists()?s.val():EMPTY;setLegacy({tasks:v.tasks||{},habits:v.habits||{},dailyRecords:v.dailyRecords||{}})}).catch(e=>setNotice(e.message))},[ctx,user]);
useEffect(()=>{const p=JSON.parse(localStorage.getItem('dayforge_places')||'null');if(p)setPlaces(p)},[]);useEffect(()=>localStorage.setItem('dayforge_places',JSON.stringify(places)),[places]);
const k=dateKey(selected),di=dayIndex(selected),todayBlocks=useMemo(()=>blocks.filter(b=>b.active!==false&&b.days?.includes(di)&&!(b.exceptions||[]).includes(k)).sort((a,b)=>a.start.localeCompare(b.start)),[blocks,di,k]);
const occupied=[...todayBlocks.map(b=>({start:mins(b.start),end:mins(b.end)})),...events.map(e=>({start:e.startMin,end:e.endMin}))],free=calcFree(occupied,420,1380,20),taskEntries=Object.entries(legacy.tasks||{}).filter(([,t])=>t.date===k),habits=Object.entries(legacy.habits||{}).filter(([,h])=>h.active!==false),done=taskEntries.filter(([,t])=>t.completed).length+habits.filter(([id])=>legacy.dailyRecords?.[k]?.habitChecks?.[id]).length,total=taskEntries.length+habits.length,percent=total?Math.round(done/total*100):0;
async function login(){try{await signInWithPopup(ctx.auth,new GoogleAuthProvider())}catch(e){setNotice(e.message)}}
async function saveLegacy(path,value){if(!ctx||!user)return;const r=ref(ctx.rtdb,`users/${user.uid}/${path}`);if(value===null)await remove(r);else await set(r,value);setLegacy(p=>{const n=JSON.parse(JSON.stringify(p)),parts=path.split('/');let q=n;for(let i=0;i<parts.length-1;i++)q=q[parts[i]]||(q[parts[i]]={});if(value===null)delete q[parts.at(-1)];else q[parts.at(-1)]=value;return n})}
async function saveBlock(b){const x={...b,title:b.title.trim()};setBlocks(p=>p.some(a=>a.id===x.id)?p.map(a=>a.id===x.id?x:a):[...p,x]);if(user)await setDoc(doc(ctx.db,'users',user.uid,'blocks',x.id),x);if(token)await syncCalendar(x,token);setModal(null);setNotice('Activity saved')}
async function removeBlock(id){setBlocks(p=>p.filter(b=>b.id!==id));if(user)await deleteDoc(doc(ctx.db,'users',user.uid,'blocks',id));setNotice('Activity removed')}
async function connectCalendar(){if(!GOOGLE_CLIENT_ID)return;setLoading(true);try{await loadScript('https://accounts.google.com/gsi/client');const tc=window.google.accounts.oauth2.initTokenClient({client_id:GOOGLE_CLIENT_ID,scope:'https://www.googleapis.com/auth/calendar',callback:r=>{if(r.error){setNotice(r.error);return}setToken(r.access_token);setConnected(true);fetchCalendar(r.access_token)}});tc.requestAccessToken({prompt:''})}catch(e){setNotice(e.message)}finally{setLoading(false)}}
async function fetchCalendar(t=token){if(!t)return;try{const a=new Date(selected);a.setHours(0,0,0,0);const b=new Date(selected);b.setHours(23,59,59,999);const r=await fetch(`https://www.googleapis.com/calendar/v3/calendars/primary/events?singleEvents=true&orderBy=startTime&timeMin=${encodeURIComponent(a.toISOString())}&timeMax=${encodeURIComponent(b.toISOString())}`,{headers:{Authorization:`Bearer ${t}`}});if(!r.ok)throw Error('Could not read Google Calendar');const j=await r.json();setEvents((j.items||[]).filter(e=>e.start?.dateTime&&e.end?.dateTime).map(e=>({id:e.id,summary:e.summary||'Calendar event',startMin:new Date(e.start.dateTime).getHours()*60+new Date(e.start.dateTime).getMinutes(),endMin:new Date(e.end.dateTime).getHours()*60+new Date(e.end.dateTime).getMinutes()})))}catch(e){setNotice(e.message)}}
async function syncCalendar(b,t){try{if(!b.days?.length)return;const tz=Intl.DateTimeFormat().resolvedOptions().timeZone,url='https://www.googleapis.com/calendar/v3/calendars/primary/events',body={summary:b.title,location:b.place,description:`DayForge ${TYPES[b.type]?.[0]||'Activity'}`,start:{dateTime:`${dateKey(selected)}T${b.start}:00`,timeZone:tz},end:{dateTime:`${dateKey(selected)}T${b.end}:00`,timeZone:tz},recurrence:[`RRULE:FREQ=WEEKLY;BYDAY=${b.days.map(i=>DAYKEY[i]).join(',')}`],extendedProperties:{private:{dayforgeId:b.id}}};const q=await fetch(`${url}?privateExtendedProperty=dayforgeId%3D${encodeURIComponent(b.id)}`,{headers:{Authorization:`Bearer ${t}`}}),j=await q.json(),old=j.items?.[0];const r=await fetch(old?`${url}/${old.id}`:url,{method:old?'PUT':'POST',headers:{Authorization:`Bearer ${t}`,'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw Error('Calendar sync failed');await fetchCalendar(t)}catch(e){setNotice(e.message)}}
return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">D</div><div><div className="brand-title">DAYFORGE</div><div className="brand-sub">Time · Place · Action</div></div></div><nav><Nav a={tab==='today'} c={()=>setTab('today')} I={LayoutDashboard}>Today</Nav><Nav a={tab==='week'} c={()=>setTab('week')} I={CalendarDays}>Week plan</Nav><Nav a={tab==='places'} c={()=>setTab('places')} I={MapPin}>Places</Nav><Nav a={tab==='settings'} c={()=>setTab('settings')} I={Settings2}>Connections</Nav><Nav a={false} c={()=>location.href='study-planner.html'} I={BookOpen}>Study planner</Nav><Nav a={false} c={()=>location.href='report.html'} I={BarChart3}>Monthly report</Nav></nav><div className="sidebar-bottom"><div className="sync-card"><div className="sync-icon">{connected?<Cloud size={16}/>:<CloudOff size={16}/>}</div><div><div className="sync-title">{connected?'Calendar linked':'Calendar not linked'}</div><div className="sync-sub">{connected?'Live day availability':'Connect to import events'}</div></div></div>{user?<button className="profile" onClick={()=>signOut(ctx.auth)}><div className="avatar">{(user.displayName||'U')[0]}</div><div className="profile-copy"><strong>{user.displayName||user.email}</strong><span>Sign out</span></div></button>:<button className="profile" onClick={login}><div className="avatar">G</div><div className="profile-copy"><strong>Sign in</strong><span>Sync to Firebase</span></div></button>}</div></aside><main className="main"><header className="topbar"><div><div className="eyebrow">YOUR DAY, DESIGNED</div><h1>{tab==='today'?selected.toLocaleDateString('en-IN',{weekday:'long',month:'long',day:'numeric'}):tab==='week'?'Weekly rhythm':tab==='places'?'Saved places':'Connections'}</h1></div><div className="top-actions"><button className="ghost" onClick={()=>setSelected(new Date())}>Today</button><button className="primary" onClick={()=>setModal('new')}><Plus size={17}/> Add block</button></div></header>{notice&&<div className="toast" onClick={()=>setNotice('')}>{notice}<X size={15}/></div>}{tab==='today'&&<Today selected={selected} setSelected={setSelected} blocks={todayBlocks} free={free} google={events} connected={connected} loading={loading} connect={connectCalendar} refresh={()=>fetchCalendar()} taskEntries={taskEntries} habits={habits} legacy={legacy} saveLegacy={saveLegacy} notes={legacy.dailyRecords?.[k]?.notes||''} percent={percent} edit={b=>setModal(b)}/>} {tab==='week'&&<Week blocks={blocks} selected={selected} edit={b=>setModal(b)}/>} {tab==='places'&&<Places places={places} add={p=>{if(p&&!places.includes(p))setPlaces(x=>[...x,p])}}/>}{tab==='settings'&&<Settings connected={connected} connect={connectCalendar}/>}</main>{modal&&<BlockModal initial={modal==='new'?null:modal} places={places} selected={selected} onClose={()=>setModal(null)} onSave={saveBlock} onAddPlace={p=>{if(p&&!places.includes(p))setPlaces(x=>[...x,p])}}/>}</div>}
function Nav({a,c,I,children}){return <button className={a?'nav active':'nav'} onClick={c}><I size={18}/>{children}</button>}
function Today({selected,setSelected,blocks,free,google,connected,loading,connect,refresh,taskEntries,habits,legacy,saveLegacy,notes,percent,edit}){const k=dateKey(selected),shift=n=>{const d=new Date(selected);d.setDate(d.getDate()+n);setSelected(d)},[taskText,setTaskText]=useState(''),[habitText,setHabitText]=useState('');return <div className="content-grid"><section className="hero-card panel"><div className="date-row"><button className="icon-btn" onClick={()=>shift(-1)}><ChevronLeft size={18}/></button><div className="date-pill"><span>{DAYS[dayIndex(selected)]}</span><strong>{selected.getDate()}</strong><span>{selected.toLocaleString('en',{month:'short'})}</span></div><button className="icon-btn" onClick={()=>shift(1)}><ChevronRight size={18}/></button><div className="calendar-connection">{connected?<><span className="dot live"/> Google Calendar connected</>:<button onClick={connect} className="link-btn">Connect Google Calendar <ArrowRight size={14}/></button>}</div></div><div className="hero-stat"><div><div className="stat-kicker">TRACKER PROGRESS</div><div className="stat-big">{percent}%</div><div className="stat-note">{taskEntries.length+habits.length} daily items · {duration(free.reduce((s,x)=>s+x.duration,0))} free</div></div><div className="spark"><Clock3 size={23}/><span>Plan the time. Choose the place. Repeat.</span></div></div><div className="timeline-head"><span>DAY TIMELINE</span>{connected&&<button className="small-btn" onClick={refresh}><RefreshCw size={14}/> Refresh Calendar</button>}</div><Timeline blocks={blocks} google={google} edit={edit}/><section className="panel legacy-panel"><div className="panel-title"><span><CheckSquare size={15}/> To-Do</span></div><div className="inputs"><input value={taskText} onChange={e=>setTaskText(e.target.value)} placeholder="What needs to be done?"/><button className="primary" onClick={async()=>{if(taskText.trim()){await saveLegacy(`tasks/t_${Date.now()}`,{title:taskText.trim(),date:k,priority:'Normal',completed:false,createdAt:Date.now()});setTaskText('')}}}><Plus size={15}/> Add</button></div>{taskEntries.map(([id,t])=><div className="legacy-row" key={id}><button className={t.completed?'check done':'check'} onClick={()=>saveLegacy(`tasks/${id}`,{...t,completed:!t.completed})}>{t.completed?'✓':'○'}</button><span className={t.completed?'done':''}>{t.title}</span><small>{t.priority||'Normal'}</small><button className="danger" onClick={()=>saveLegacy(`tasks/${id}`,null)}><Trash2 size={14}/></button></div>)}</section><section className="panel legacy-panel"><div className="panel-title"><span><CircleDot size={15}/> Habits</span></div><div className="inputs"><input value={habitText} onChange={e=>setHabitText(e.target.value)} placeholder="Add a habit"/><button className="primary" onClick={async()=>{if(habitText.trim()){await saveLegacy(`habits/h_${Date.now()}`,{name:habitText.trim(),goal:'Daily',active:true,createdAt:Date.now()});setHabitText('')}}}><Plus size={15}/> Add</button></div>{habits.map(([id,h])=>{const ok=!!legacy.dailyRecords?.[k]?.habitChecks?.[id];return <div className="legacy-row" key={id}><button className={ok?'check done':'check'} onClick={()=>saveLegacy(`dailyRecords/${k}/habitChecks/${id}`,ok?null:true)}>{ok?'✓':'○'}</button><span>{h.name}</span><small>{h.goal||'Daily'}</small><button className="danger" onClick={()=>saveLegacy(`habits/${id}`,null)}><Trash2 size={14}/></button></div>})}</section><section className="panel legacy-panel"><div className="panel-title"><span><StickyNote size={15}/> Daily notes</span><button className="small-btn" onClick={()=>saveLegacy(`dailyRecords/${k}/notes`,document.getElementById('day-notes').value)}>Save</button></div><textarea id="day-notes" defaultValue={notes} placeholder="Notes for this day…" rows="4"/></section></section><aside className="side-stack"><div className="panel free-panel"><div className="panel-title"><span>Free windows</span><Clock3 size={17}/></div>{free.map((x,i)=><div className="free-row" key={i}><strong>{x.start} – {x.end}</strong><span className="duration">{duration(x.duration)}</span></div>)}{!free.length&&<div className="empty">No free windows ≥ 20 min.</div>}</div><div className="panel focus-panel"><div className="panel-title"><span>Next block</span><MapPin size={17}/></div>{blocks[0]?<div className="next-card"><div className="next-time">{blocks[0].start}</div><div><h3>{blocks[0].title}</h3><span><MapPin size={13}/>{blocks[0].place}</span></div></div>:<div className="empty">Nothing scheduled yet.</div>}</div></aside></div>}
function Timeline({blocks,google,edit}){const all=[...blocks.map(b=>({...b,startMin:mins(b.start),endMin:mins(b.end),source:'plan'})),...google.map(e=>({...e,startMin:e.startMin,endMin:e.endMin,source:'google',title:e.summary,place:'Google Calendar'}))].sort((a,b)=>a.startMin-b.startMin);return <div className="timeline">{Array.from({length:17},(_,i)=>i+7).map(h=><div className="hour" key={h}><span>{h>12?h-12:h}:00 {h<12?'AM':'PM'}</span><div className="hour-line"/></div>)}{all.map((b,i)=><button key={b.id||i} className={`block-card ${b.color||'google'}`} style={{top:`${(b.startMin-420)*2.65}px`,height:`${Math.max(42,(b.endMin-b.startMin)*2.65)}px`}} onClick={()=>b.source==='plan'&&edit(b)}><div className="block-left"><strong>{b.title}</strong><span><MapPin size={12}/>{b.place}</span></div><span className="block-time">{clock(b.startMin)}–{clock(b.endMin)}</span></button>)}</div>}
function Week({blocks,selected,edit}){const s=new Date(selected);s.setDate(s.getDate()-dayIndex(s));return <div className="week-panel panel"><div className="week-summary"><div><div className="stat-kicker">WEEKLY RHYTHM</div><h2>Set once. Repeat reliably.</h2><p>Your real saved schedule. No demo blocks.</p></div><button className="primary" onClick={()=>edit(null)}><Plus size={17}/> Add recurring block</button></div><div className="week-grid">{DAYS.map((d,i)=>{const bs=blocks.filter(b=>b.active!==false&&b.days?.includes(i)).sort((a,b)=>a.start.localeCompare(b.start));return <div className="day-col" key={d}><div className="day-head"><span>{d}</span><strong>{new Date(s.getFullYear(),s.getMonth(),s.getDate()+i).getDate()}</strong></div>{bs.map(b=><div className="week-block" key={b.id} onClick={()=>edit(b)}><div className={`mini-dot ${b.color||''}`}/><div><strong>{b.title}</strong><span>{b.start}–{b.end}</span><span><MapPin size={11}/>{b.place}</span></div></div>)}{!bs.length&&<div className="week-empty">No blocks</div>}</div>})}</div></div>}
function Places({places,add}){const[v,setV]=useState('');return <div className="places-grid"><div className="panel place-hero"><div className="stat-kicker">QUICK LOCATION PICKER</div><h2>Use places as cues.</h2><p>Save familiar places so creating a block takes seconds.</p><div className="place-input"><MapPin size={17}/><input value={v} onChange={e=>setV(e.target.value)} placeholder="Add custom place…"/><button onClick={()=>{if(v.trim()){add(v.trim());setV('')}}}><Plus size={16}/></button></div></div><div className="panel place-list"><div className="panel-title"><span>Recent & common places</span><span className="muted">{places.length} saved</span></div><div className="place-cards">{places.map(p=>{const C=COMMON.find(x=>x[0]===p)?.[1]||MapPin;return <div className="place-card" key={p}><div className="place-icon"><C size={17}/></div><div><strong>{p}</strong><span>Quick pick</span></div></div>})}</div></div></div>}
function Settings({connected,connect}){return <div className="settings-wrap"><div className="panel settings-card"><div className="settings-title"><Settings2/><div><div className="stat-kicker">BACKEND & SYNC</div><h2>Connections</h2></div></div><div className="setting-row"><div><strong>Firebase</strong><span>Authentication + Firestore schedule + Realtime Database tracker</span></div><span className="status ok">READY</span></div><div className="setting-row"><div><strong>Google Calendar</strong><span>{connected?'Connected for this browser session':'Connect to import events and sync blocks'}</span></div><button className="outline" onClick={connect}>{connected?'Reconnect':'Connect Google Calendar'}</button></div><div className="security-note"><ShieldCheck size={18}/><div><strong>Private data</strong><p>Each user's tracker stays under their Firebase user path.</p></div></div></div></div>}
function BlockModal({initial,places,selected,onClose,onSave,onAddPlace}){const[f,setF]=useState(initial||{id:crypto.randomUUID(),title:'',type:'study',start:'16:30',end:'17:30',place:places[0]||'Study Desk',days:[0,1,2,3,4],color:'cyan',active:true,exceptions:[]}),[ex,setEx]=useState(dateKey(selected)),setv=(k,v)=>setF(x=>({...x,[k]:v}));return <div className="modal-backdrop"><div className="modal"><div className="modal-head"><div><div className="stat-kicker">TIME · PLACE · ACTION</div><h2>{initial?'Edit block':'Create a block'}</h2></div><button className="icon-btn" onClick={onClose}><X/></button></div><div className="form"><label>Activity name<input value={f.title} onChange={e=>setv('title',e.target.value)} placeholder="Coding study"/></label><div className="two"><label>Start<input type="time" value={f.start} onChange={e=>setv('start',e.target.value)}/></label><label>End<input type="time" value={f.end} onChange={e=>setv('end',e.target.value)}/></label></div><label>Type<div className="seg-row">{Object.entries(TYPES).map(([k,[n,C]])=><button type="button" key={k} className={f.type===k?'seg active':'seg'} onClick={()=>setv('type',k)}><C size={14}/>{n}</button>)}</div></label><label>Place<div className="place-select">{places.map(p=><button type="button" key={p} className={f.place===p?'place-chip active':'place-chip'} onClick={()=>setv('place',p)}>{p}</button>)}<button type="button" className="place-chip add" onClick={()=>{const p=prompt('Place name');if(p){onAddPlace(p);setv('place',p)}}}><Plus size={13}/> New</button></div></label><label>Repeat on<div className="day-row">{DAYS.map((d,i)=><button type="button" key={d} className={f.days.includes(i)?'day active':'day'} onClick={()=>setv('days',f.days.includes(i)?f.days.filter(x=>x!==i):[...f.days,i])}>{d}</button>)}</div></label><label>Leave / exception date<div className="exception-row"><input type="date" value={ex} onChange={e=>setEx(e.target.value)}/><button type="button" className="small-btn" onClick={()=>setv('exceptions',Array.from(new Set([...(f.exceptions||[]),ex])))}>Cancel this date</button></div><div className="exception-chips">{(f.exceptions||[]).map(d=><button type="button" key={d} className="exception-chip" onClick={()=>setv('exceptions',f.exceptions.filter(x=>x!==d))}>{d}<X size={11}/></button>)}</div></label><div className="modal-foot"><button className="ghost" onClick={onClose}>Cancel</button><button className="primary" onClick={()=>{if(mins(f.end)<=mins(f.start))return alert('End time must be after start time.');if(!f.days.length)return alert('Select at least one day.');onSave(f)}}><Save size={16}/>{initial?'Save changes':'Create block'}</button></div></div></div></div>}
function calcFree(items,start,end,gap){let cur=start,out=[];for(const x of items){const s=Math.max(start,x.start),e=Math.min(end,x.end);if(s>cur&&s-cur>=gap)out.push({start:clock(cur),end:clock(s),duration:s-cur});if(e>cur)cur=e}if(end-cur>=gap)out.push({start:clock(cur),end:clock(end),duration:end-cur});return out}
function loadScript(src){return new Promise((resolve,reject)=>{if(document.querySelector(`script[src="${src}"]`))return resolve();const s=document.createElement('script');s.src=src;s.onload=resolve;s.onerror=()=>reject(new Error(`Could not load ${src}`));document.head.appendChild(s)})}
createRoot(document.getElementById('root')).render(<App/>);
